apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexushub.fullname" . }}
  labels: {{- include "nexushub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "nexushub.selectorLabels" . | nindent 4 }}
